node_modules
.DS_Store
dist
dist-ssr
*.local
.env
.env.prod
.nitro
.tanstack
.wrangler
.output
.vinxi
__unconfig*
todos.json
.pnpm-store
.pnpm

# Agent UI verification screenshots (ephemeral)
.cursor/ui-verify/

# Local-only audit notes + prod data snapshots — contain user financial PII, never commit
docs/audit/

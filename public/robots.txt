# https://www.robotstxt.org/robotstxt.html
# Public app — allow crawling. Only block the API + auth-gated app routes
# (those redirect to sign-in and hold no indexable content).
User-agent: *
Allow: /
Disallow: /api/

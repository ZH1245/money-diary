# recurring — agent context

**Read the module knowledge file before editing this feature:**
→ [`docs/modules/recurring.md`](../../../docs/modules/recurring.md)

Then the global rules + stack + boundaries: [`AGENTS.md`](../../../AGENTS.md).

Follow the feature anatomy and the server-only boundary (DB/secrets only in `server/`)
described there. Validate external input with the Zod schemas in `schemas/`.

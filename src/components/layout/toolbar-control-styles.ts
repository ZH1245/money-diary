/**
 * Padding-based toolbar controls — no fixed width/height boxes.
 * Icon-only: p-2. With label (xl+): px-4 py-2.
 */
export const toolbarIconButtonClass = 'p-2'

/**
 * Responsive toolbar button: compact icon pad, wider horizontal pad when label shows.
 */
export const toolbarExpandableButtonClass = 'p-2 xl:px-4 xl:py-2'
